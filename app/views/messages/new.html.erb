<div class="page-header">
  <h1>Send [Person] a message!</h1>
</div>

<h1>Message to <%= @user %></h1>

##sender userid should autopopulate based off of logged in user; receiver userid and info should autopopulate based on person selected on prior tab bc their id was passed through

<%= form_tag(messages_url, method: 'post', class: 'form-horizontal') do %>
  
  <%= hidden_field_tag :receiver_userid, params[:id] %>

  <div class="form-group">
    <%= label_tag :message, nil, class: 'col-md-2 control-label' %>
    <div class="col-md-6">
      <%= text_area_tag :message, nil, class: 'form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-md-offset-2 col-md-6">
      <%= submit_tag "Send your question", class: 'btn btn-primary' %>
    </div>
  </div>
<% end %>
